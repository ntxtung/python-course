"""
Write a function quadratic_equation_solver that solve ax^2 + bx + c = 0:
Input: (a: float, b: float, c: float)
Output: root x1, x2 (in list), return None if no root or infinity roots

Scope: We don't work with complex number. Not yet! 🥲
"""


def quadratic_equation_solver(a: float, b: float, c: float) -> list[float, float] | None:
    return None
